import SwiftUI

struct TeleportTheme {
    static let primaryColor = Color(red: 0.2, green: 0.47, blue: 0.85)
    static let backgroundColor = Color(.systemBackground)
    static let secondaryBackground = Color(.secondarySystemBackground)
    static let textPrimary = Color(.label)
    static let textSecondary = Color(.secondaryLabel)
    static let bubbleOutgoing = Color(red: 0.23, green: 0.53, blue: 0.88)
    static let bubbleIncoming = Color(.secondarySystemBackground)
    static let separator = Color(.separator)

    static let cornerRadius: CGFloat = 12
    static let messagePadding: CGFloat = 10
}
